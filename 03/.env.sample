# ------------------------------------------------------------------
# Application environment: dev | staging | prod
ENV=dev

# Your application name (displayed in Swagger UI)
APP_NAME=FastAPI Library

# Semantic version of the public API
API_VERSION=1.0.0

# Where the database lives (keep sqlite for local use)
DATABASE_URL=sqlite:///./library.db

# Logging verbosity: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
# ------------------------------------------------------------------